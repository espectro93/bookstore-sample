type Book {
    id: String!
    title: String!
    authors: [String!]
    publishDate: String
    pages: Int!
    isbn: String!
    publisherName: String!
    inStock: Boolean!
}

type Query{
    getBook(bookId: String!): Book!
    getBooksPaged(page: Int, size: Int): [Book]
}